{
  "name": "wa-assistant-monorepo",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev:frontend": "npm --prefix frontend run dev",
    "dev:backend": "python -m uvicorn backend.main:app --reload --host 0.0.0.0 --port 8000"
  },
  "workspaces": [
    "frontend"
  ]
}
